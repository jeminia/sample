machine:
  timezone: Asia/Tokyo
  environment:
dependencies:
  pre:
    - sudo pip install awscli
  override:
  post:
test:
   override:
     - echo "circle ci test"
deployment:
   deploy:
    branch: master
    commands:
      - sh s3test.sh
